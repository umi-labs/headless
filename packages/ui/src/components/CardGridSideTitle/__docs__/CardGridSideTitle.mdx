import { Canvas, Meta } from "@storybook/blocks";
import Example from "./example.tsx";
import * as CardGridSideTitle from "./CardGridSideTitle.stories.tsx";

<Meta of={ CardGridSideTitle } title="CardGridSideTitle" />

# Hero 1

Basic component with different props.

## Example

<Canvas of={ CardGridSideTitle.Primary} />

#### Arguments

- **title** _`string`_ - A string that represents the text content of the button.
