import { Canvas, Meta } from "@storybook/blocks";
import Example from "./example.tsx";
import * as TestimonialsCarousel from "./TestimonialsCarousel.stories.tsx";

<Meta of={ TestimonialsCarousel } title="TestimonialsCarousel" />

# Hero 1

Basic component with different props.

## Example

<Canvas of={ TestimonialsCarousel.Primary} />

#### Arguments

- **title** _`string`_ - A string that represents the text content of the button.
