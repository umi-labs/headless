import { Canvas, Meta } from '@storybook/blocks';
import Example from './example.tsx';
import * as SlidingContent from './SlidingContent.stories.tsx';

<Meta of={SlidingContent} title="SlidingContent" />

# Sliding Content

Sliding cards with intro content

## Example

<Canvas of={SlidingContent.Primary} />

#### Arguments

- **title** _`string`_ - A string that represents the text content of the button.
