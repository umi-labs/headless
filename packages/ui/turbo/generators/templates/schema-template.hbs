import { z } from "zod";

export const Schema = z.object({
data: z.object({
title: z.string(),
}),
});

export type Type = z.infer
<typeof Schema>;

  export const TEST_DATA: Type["data"] = {
  title: "Test",
  };
